export enum RepositoryCommonSettingValidator {
  REQUIRED = 'required',
  MAX = 'max',
  MIN = 'min',
  PATTERN = 'pattern',
  MAX_LENGTH = 'maxLength',
  MIN_LENGTH = 'minLength',
  INTEGER = 'integer'
}

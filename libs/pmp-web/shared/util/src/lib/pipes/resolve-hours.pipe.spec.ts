import { ResolveHoursPipe } from './resolve-hours.pipe';

describe('ResolveHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
